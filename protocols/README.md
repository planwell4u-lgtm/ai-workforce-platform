# Protocols

`protocols/` contains versioned public APIs, events, schemas, and generated-code inputs. It must not contain runtime secrets, provider clients, business policy, or persistence implementations.

The first contracts will be introduced with B1 after the relevant owners confirm tenant, authorization, and compatibility requirements.

